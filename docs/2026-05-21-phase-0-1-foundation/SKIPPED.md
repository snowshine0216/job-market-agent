# SKIPPED items

No items skipped. This is a single-feature spec run (N=1); all in-scope work for Phase 0 + Phase 1 is captured in [items/001-spec.md](items/001-spec.md). Items deferred to later phases (Randstad, Bing, browser fallback, LLM extraction, narration, multi-source orchestration, `jma sources status` / `jma report` / `jma run` CLI verbs, alias YAMLs) are out-of-scope by the source spec itself (§1 "Out of scope") and will be addressed by future spec runs.
