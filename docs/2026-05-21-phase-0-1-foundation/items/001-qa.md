Verdict: PASS

## Round 2 — re-verify after fix-round 1

Re-running QA after the 4-latent + 2-nit + 1-QA-nit fix round. Commits verified:
- `78b55e3` fix(pipeline): wire cache.get into the crawl loop (L1)
- `c7754f3` refactor(normalize): extract helpers (nit)
- `8f926f6` test(dedup): pin canonical_id and job_id hex values (QA nit)

Branch: `claude/phase-0-1-foundation-001` (HEAD `8f926f6`).

## Test command result (count + ruff)

Command: `uv run pytest`

```
93 passed, 1 deselected, 3 warnings in 16.71s
```

- Pass count: **93** (up from 84 in round 1; 9 new tests added)
- Deselected: **1** (live test, excluded via `-m "not live"` default)
- Failures/errors: **0**

Command: `uv run ruff check .`

Output: `All checks passed!` Exit code: **0**.

## Adversarial checks

### Check #1 — Break cache-read path

**Mutation:** In `src/jma/pipeline/crawl.py`, replaced `return await urlcache.get(db, url=url)` with `hit = None  # FORCE re-fetch; return hit`.

**Test run:** `uv run pytest tests/pipeline/test_crawl_e2e.py::test_second_run_hits_cache_for_fresh_urls -x`

**Result:** FAILED — `AssertionError: page 1 was re-fetched despite fresh cache entry` (`route1.call_count == 2`, expected `1`). Confirms test correctly detects the broken cache path.

**After revert:** test PASSES. Cache-read path is genuinely exercised.

---

### Check #2 — Break exception-finalise path

**Mutation:** In `src/jma/pipeline/crawl.py`, commented out `await finish_run(db, run_id=run_id, source_results=[error_result])` inside the `except` branch.

**Test run:** `uv run pytest tests/pipeline/test_crawl_e2e.py::test_unhandled_source_exception_still_finishes_run -x`

**Result:** FAILED — `AssertionError: finished_at must be set even after an exception` (`finished_at is None`). Confirms the test correctly detects the dangling `runs` row.

**After revert:** test PASSES. Exception-finalise path is genuinely exercised.

---

### Check #3 — Break L4 blob gate

**Mutation:** In `src/jma/sources/testerhome.py`, removed the `if status_code == 200:` guard and wrote blob unconditionally.

**Test run:** `uv run pytest tests/sources/test_testerhome.py::test_blob_not_written_on_blocked_response -x`

**Result:** FAILED — `AssertionError: Expected no blobs but found [... f578a8ec9719faf4.html.gz]`. Confirms the test correctly detects the unguarded blob write on non-200 responses.

**After revert:** test PASSES. Blob gate is genuinely exercised.

## Acceptance criteria walkthrough (§10)

### §10.1 — `uv run pytest` exits 0 with live marker skipped

**PASS.** `93 passed, 1 deselected, 3 warnings in 16.71s`. Exit code 0. Deselected is the `@pytest.mark.live` test.

### §10.2 — `tests/live/test_testerhome_live.py` exists and is `@pytest.mark.live`

**PASS.** File exists at `tests/live/test_testerhome_live.py`. Confirmed `@pytest.mark.live` decorator is present on the test function. Not run (rate-limit etiquette).

### §10.3 — Pipeline e2e covers `runs`, `jobs`, `run_jobs`, blob, `url_cache`; second-run cache invariants hold

**PASS.** `test_end_to_end_writes_runs_jobs_run_jobs_cache_blob` asserts all five storage invariants:
- `runs` row exists for run_id
- `jobs` count >= 1 with non-empty `canonical_id`
- `run_jobs` edge count == jobs count
- blob `.gz` file present under `raw/testerhome/`
- `url_cache` has at least one row with `status_code=200`

`test_second_run_hits_cache_for_fresh_urls` additionally asserts the same invariants hold on the second run (status OK, jobs >= 1) while confirming zero re-fetches from the HTTP layer.

### §10.4 — `uv run ruff check` clean

**PASS.** Exit code 0, `All checks passed!`.

### Pinned-hash sanity (§10 / QA nit)

**PASS.** `tests/domain/test_dedup.py` lines 62–71 contain both:
- `test_canonical_id_pinned_value` — asserts `canonical_id(...)` == `"445e9bf368e83676a23c3e15a0bfc17c886fa244"` (40-char sha1)
- `test_job_id_with_internal_id_pinned_value` — asserts `job_id(...)` == `"29fbf773510b7b61753b8fd080385e27cdf11576"` (40-char sha1)

## Anything regressed

None. All 93 tests pass, ruff is clean, adversarial mutations all detected and reverted cleanly.
