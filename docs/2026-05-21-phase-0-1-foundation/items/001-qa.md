Verdict: PASS

## Test command result

Command: `uv run pytest`

```
84 passed, 1 deselected, 3 warnings in 16.68s
```

- Pass count: **84** (meets spec §10 requirement of 84+)
- Deselected: **1** (live test, correctly excluded via `-m "not live"` default)
- Failures/errors: **0**

### Adversarial mutation result

**Mutation tried:** Removed NFKC normalization from `_normalize_for_match` in `src/jma/domain/normalize.py` (changed `_nfkc(s).lower()` to `s.lower()`), then ran `uv run pytest tests/domain/test_dedup.py`.

**Result:** 2 tests failed as expected:
- `test_job_id_fallback_nfkc_and_whitespace_collapse` — full-width ＡＩ no longer collapsed to ASCII AI
- `test_canonical_id_normalises_inputs` — same failure

After revert, all 8 dedup tests pass again. **The NFKC invariant is genuinely exercised.**

**Side finding (NIT, not a blocker):** Changing the join separator in `canonical_id` from `|` to `,` (a weaker mutation) does NOT fail any test. The dedup tests only verify that equal inputs produce equal hashes — they do not pin the specific serialization format. This means a future refactor changing the separator character would go undetected. Recommend adding one pinned-hash test.

## Ruff result

Command: `uv run ruff check .`

Exit code: **0**. Output: `All checks passed!`

## Acceptance criteria walkthrough

### §10.1 — `uv run pytest` exits 0 with live marker skipped

**PASS.** Output: `84 passed, 1 deselected, 3 warnings in 16.68s`. Exit code 0. The deselected test is the `@pytest.mark.live` test, excluded by the `addopts = -m "not live"` default in `pyproject.toml`.

### §10.2 — `pytest -m live` green on maintainer's machine

**Manual verification only (not run by QA — rate-limit etiquette).** File exists at `tests/live/test_testerhome_live.py`. The test function `test_testerhome_live_smoke` is decorated with both `@pytest.mark.live` and `@pytest.mark.asyncio`. It asserts `result.status.value == "ok"` and `len(result.jobs) >= 1`. The file is correctly structured for maintainer opt-in.

### §10.3 — Pipeline e2e covers `runs`, `jobs`, `run_jobs`, blob, `url_cache`

**PASS.** `tests/pipeline/test_crawl_e2e.py::test_end_to_end_writes_runs_jobs_run_jobs_cache_blob` asserts:

- **(a)** `runs` row: `SELECT COUNT(*) FROM runs WHERE id=?` → asserts `== 1`
- **(b)** `jobs` row count ≥1 with non-empty `canonical_id`: `SELECT COUNT(*) FROM jobs` → `assert n_jobs >= 1`; `SELECT canonical_id FROM jobs LIMIT 1` → `assert canon != ""`
- **(c)** `run_jobs` edges: `SELECT COUNT(*) FROM run_jobs WHERE run_id=?` → `assert count == n_jobs`
- **(d)** blob present: `blobs_dir.rglob("*")` → `assert any(p.suffix == ".gz" …)`
- **(e)** `url_cache` populated: `SELECT COUNT(*) FROM url_cache WHERE status_code=200` → `assert >= 1`

All five required assertions are present.

### §10.4 — `uv run ruff check` clean

**PASS.** Exit code 0, `All checks passed!`.

## Adversarial findings

### Check #1 — Mutation: NFKC normalization removal

Mutated `src/jma/domain/normalize.py` line 16 to skip `_nfkc()`. Ran `uv run pytest tests/domain/test_dedup.py`. **2 tests failed** (`test_job_id_fallback_nfkc_and_whitespace_collapse`, `test_canonical_id_normalises_inputs`). Reverted. All 8 tests pass. **The NFKC dedup invariant is genuinely tested.**

### Check #2 — Salary disclosure three-way property

Ran:
```python
uv run python -c "
from jma.domain.models import Salary, SalaryPeriod
assert Salary(raw='', parsed=False).disclosure == 'absent'
assert Salary(raw='面议', parsed=False).disclosure == 'unparseable'
assert Salary(raw='15-30K', parsed=True, min=15000, max=30000, period=SalaryPeriod.MONTHLY).disclosure == 'parseable'
print('disclosure OK')
"
```
Output: `disclosure OK`. **All three disclosure states work correctly.**

### Check #3 — PartialHarvest convention (spec §2 row 9)

Inspected `tests/sources/test_testerhome.py::test_partial_harvest_on_429_after_page_1` (lines 113–126). The test asserts:
- `result.status == SourceStatus.OK` — line 123
- `result.reason.startswith("partial:")` — line 124

Both properties are asserted. Additionally, `"page 2" in result.reason` is verified at line 125. **PartialHarvest convention is correctly tested.**

## Notes / nits

### NIT-1: pytest collection warnings for `TesterHomeSource` (cosmetic, not a blocker)

3 warnings appear in every test run:
```
PytestCollectionWarning: cannot collect test class 'TesterHomeSource' because it
has a __init__ constructor (from: tests/live/…, tests/pipeline/…, tests/sources/…)
```
The class name starts with "Test", causing pytest to attempt collection. Recommend renaming to `TesterHomeJobSource` or adding `__test__ = False` at class level in `src/jma/sources/testerhome.py`. Classify: **NIT**.

### NIT-2: Separator mutation not caught by dedup tests

Changing `|` to `,` in `canonical_id`'s f-string payload does not fail any test (verified during adversarial check). The tests only verify equal-inputs-equal-hash, not a pinned hash value. Consider adding one pinned-hash assertion like:
```python
assert canonical_id(title="AI Engineer", company="Foo", city="Hangzhou") == "<known_sha1>"
```
This would also catch accidental schema drift. Classify: **NIT**.
