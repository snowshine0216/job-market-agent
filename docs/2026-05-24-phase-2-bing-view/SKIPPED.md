# SKIPPED

No run-level OUT items — spec mode has exactly one IN row. The source spec's own "Out of scope (deferred)" list (§1) is enforced *within* the plan phase, not at the MASTER-SPEC layer; the Opus plan subagent must respect those exclusions verbatim.
